[package]
name = "order_service_api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.6"
tokio = {version = "1.12", features = ["full"]}
serde = {version = "1.0", features = ["derive"]}
serde_json = "1"
